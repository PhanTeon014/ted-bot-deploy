import type makeWASocket from '../Socket/index.js';
import type { ILogger } from './logger.js';
type WASocket = ReturnType<typeof makeWASocket>;
export type SignalStreamOptions = {
    url: string;
    logger?: ILogger;
};
export declare const listenToSignalStream: (sock: WASocket, opts?: Partial<SignalStreamOptions>) => () => void;
export {};
