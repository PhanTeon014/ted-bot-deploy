import type makeWASocket from '../Socket/index.js';
type WASocket = ReturnType<typeof makeWASocket>;
export type ParsedWhatsAppChannelLink = {
    channelId: string;
    jid: string;
    serverId: string | undefined;
    emojis: string[];
};
export declare const parseWhatsAppChannelLink: (text: string) => ParsedWhatsAppChannelLink;
export type SocketRef = {
    id?: string | number;
    sock: WASocket;
};
export type ReactToChannelResult = {
    total: number;
    reacoes: Array<{
        id: string | number;
        emoji: string;
        ok: boolean;
        erro?: string;
    }>;
};
export declare const reactToChannelAcrossSockets: (sockets: SocketRef[], opts: {
    jid: string;
    serverId: string;
    emojis: string[];
}) => Promise<ReactToChannelResult>;
export type FollowChannelResult = {
    total: number;
    sucesso: number;
    falhas: Array<{
        id: string | number;
        erro?: string;
    }>;
};
export declare const followChannelAcrossSockets: (sockets: SocketRef[], jid: string) => Promise<FollowChannelResult>;
